{
  "name": "@enneo/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for the Enneo platform — exposes ticket, AI agent, customer, and configuration tools with OAuth-authenticated access.",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "enneo-mcp-server": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "open": "^10.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
